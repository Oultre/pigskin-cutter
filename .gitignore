# --- video: never commit raw film -------------------------------------------
# Game film runs into the hundreds of GB. It lives on disk, not in git history.
*.mp4
*.mov
*.mkv
*.avi
*.m4v
*.ts
*.wmv
*.webm

# --- generated output -------------------------------------------------------
/output/
/clips/
/cache/
*.sqlite
*.sqlite3
*.db
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
library.lock

# --- scraped pages: cached locally, not committed ---------------------------
/cache/pbp/

# --- python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# --- node / frontend --------------------------------------------------------
node_modules/
frontend/dist/
.vite/
# built UI (generated from frontend/ by `npm run build`; shipped via PyInstaller)
/src/cutup/web/static/

# --- os / editor ------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- secrets ----------------------------------------------------------------
.env
.env.*
!.env.example
# scratch/demo libraries created during local testing
/scratch-*/
